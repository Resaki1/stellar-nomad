# docs:references

## SUMMARY
Mined all six requested docs (plus CLOUD_REVIEW_2026-07.md, Frostbite.md and the KSP/EVE mod folder) for constraints on CLOUD_TYPES_PLAN.md. The project's own history strongly VALIDATES the new plan's core moves (independent type channel, per-step weather sampling, km-anchoring, separate cirrus layer, LUT profiles — Frostbite.md even documents a literal 2D type-profile LUT texture with x=type, y=height, R=density, G=erosion-amount, which is primary-source support for §4.2 and suggests an optional LUT G-channel alternative to §4.3's ALU ramps). But the docs also encode ~20 hard-won constraints the migration can silently violate: TSL If-scope magic and .toVar aliasing (Phase 0 refactor), the coverage-cache/outer-gate audit family (Phase 1 weatherAt struct), smoothstep-on-noise bimodal banding (the OLD topAlt formula produced visible stripes twice — the new topHeight decode and bake-time noise injection must use linear/wide remaps), gate-integrand coincidence (Phase 1/2 LUT swap), envelope-erosion architecture + flat-base Remap-survival (Phase 2), edges-want-low-freq + amplitude-fade rules (Phase 3), forced .level(0) on all marcher taps vs auto-mip on shell taps (weather map v2 + cirrus texture), and the three@0.183.2 Data3DTexture mip patch that must be re-verified on any three upgrade.

Two genuine discrepancies with VOLUMETRIC_CLOUDS_SHAPE_PLAN.md exist: (1) its §2 "locked decision" was procedural-synthesis-first with authored textures only as a per-planet override — the new plan inverts this for Earth (ERA5 data bake as baseline); defensible via R4 but it reverses a recorded locked decision and should be surfaced to Christian explicitly. (2) Its planned channel layout (R=coverage, G=type, B=density/wetness — also VOLUMETRIC_CLOUDS_PLAN B2 Stage 2's R/G/B/A = coverage/type/height-bias/wetness) is superseded by coverage/convectivity/topHeight/cirrus; wetness/precip had no consumer then and is correctly deferred to "weather map B" now. A third, softer staleness: SHAPE_PLAN's anti-tiling decision (accept 20 km tiling, park detile) was premised on the volumetric rendering to 1500-3000 km; the distance-based shell handoff has since LANDED (SHELL_HANDOFF_FAR_KM caps the march), which is exactly the condition under which SHAPE_PLAN said detile/lower-crossfade options "become cheap and worthwhile" — worth a one-line revisit note, not a blocker.

The debugging-lessons file maps cleanly onto the migration phases: Phase 0 re-triggers the If-scope/toVar/delete-superseded-paths lessons (falsify with pixel-parity + full DEBUG_VIZ sweep); Phase 1 re-triggers the coverage-cache/outer-gate family (#1 + follow-ons), bimodal-banding, and case #13 gate lessons (falsify with alpha/iters/firstHit/whyStop/maxProfile/maxProbeShape); Phase 2 re-triggers case #20 floaters and the flat-base "lava lamp" anatomy plus light-volume lockstep #17/18 (falsify with floaterProbe/baseColumn + the noise histogram + shadow-attachment check + the STILL-OPEN shell Bug A altitude-sampling issue); Phase 3 re-triggers case #21 speckle/pockmark and case #22 distant-flicker (falsify with eroded viz + A/B toggles + the fineDelta amplitude-fade check); the slab raise re-triggers slabLen-derived march tuning and case #22's tExitSlab geometry (falsify with whyStop/iters at 3 canonical positions); the cirrus shell re-triggers the review's OPEN Bug B flicker mechanism (fresh-every-frame shell + sub-pixel content), the case #18(c) mean-shift rule, and the depressed-horizon terminator rule.

## SHAPE_PLAN locked decision 'procedural maps, not hand-authored' is REVERSED by the new plan's Earth data bake — surface to Christian [confirmed]
VOLUMETRIC_CLOUDS_SHAPE_PLAN.md §2 'Key decisions (locked this session)' item 1 (2026-06-14): 'Synthesise weather/cloud-type maps procedurally (not hand-authored textures)' because the game streams planet→orbit with no loading screens and must generalise; 'An authored-texture override may be added per-planet later, but is not the baseline.' CLOUD_TYPES_PLAN §4.7/§5 Phase 4 makes Earth's baseline a baked ERA5 texture (procedural becomes the R4 path for OTHER planets). The new plan honors the spirit (same consumption path, shared classifier §4.8) but formally inverts a recorded locked decision for Earth. Note the new plan's own Phase ordering mitigates: Phases 1-3 use a SYNTHETIC map, so the procedural path is exercised first. Constrains: §4.1, §4.7, §6 open questions (add this as an explicit question).
ANCHORS: docs/VOLUMETRIC_CLOUDS_SHAPE_PLAN.md §2 item 1 | docs/CLOUD_TYPES_PLAN.md §4.7, §5 Phase 1/4

## Channel-layout discrepancy: both old plans specced G=type/B=density-or-heightBias/A=wetness — new layout supersedes; wetness correctly dropped (it never had a consumer) [confirmed]
SHAPE_PLAN §2 item 1 planned 'R = coverage, G = cloud type, B = density / wetness'. VOLUMETRIC_CLOUDS_PLAN.md B2 Stage 2 planned RGBA = coverage / type / height-offset bias / wetness, and its Risks section already said 'Wetness is encoded in the weather-map spec but not used by anything in this plan… If unused at E2 time, drop it from the spec.' The new layout (coverage/convectivity/topHeight/cirrus) supersedes both; precip/optical-depth deferral to 'weather map B' (§4.1) is consistent with the old wetness-had-no-consumer finding. B=topHeight is a strict upgrade of the old 'height-offset bias'. No conflict to resolve, but CLOUD_TYPES_PLAN should note it supersedes SHAPE_PLAN §2.1 and VOLUMETRIC_CLOUDS_PLAN B2-Stage-2 so future readers don't treat those as live specs.
ANCHORS: docs/VOLUMETRIC_CLOUDS_SHAPE_PLAN.md §2, §7 Phase C.1 | docs/VOLUMETRIC_CLOUDS_PLAN.md §Phase B B2 + §Risks 'Wetness channel' | docs/CLOUD_TYPES_PLAN.md §4.1

## Frostbite.md is primary-source support for the profile LUT — and stores per-type EROSION AMOUNT as a second LUT channel (alternative to §4.3's ALU ramps) [confirmed]
Frostbite.md §5.3.1 (~lines 955-990): weather texture R=2D density, G=cloud type index; the type indexes a separate 'cloud type texture' with x=type, y=normalised height in layer, R=density-along-height, G='erosion amount applied (0=smooth, 1=fully eroded by the 3D erosion texture)'. 'They can for instance represent multiple layers of clouds or Anvil clouds.' Artists asked to keep these textures small for cache. This is exactly CLOUD_TYPES_PLAN §4.2's LUT (independently of Nubis 2022) AND suggests an option: put the FINE_CARVE/wispiness ramp in a LUT G channel (freely authorable per genus, e.g. cirrus fibrousness) instead of hardcoded lerp(a,b,convectivity) ALU ramps — same fetch, more art control. Also confirms §3.5's 'Frostbite has a separate cheap panning 2D high-cloud layer'. Constrains/informs: §4.2, §4.3.
ANCHORS: docs/VolumetricCloudReferences/Frostbite.md §5.3.1 lines ~955-990 | docs/CLOUD_TYPES_PLAN.md §4.2, §4.3

## smoothstep-on-noise bimodal banding: the OLD topAlt formula produced visible stripes TWICE — hard constraint on the topHeight channel decode and bake-time tower-noise injection [confirmed]
Two independent past incidents: (1) CLOUD_DEBUGGING_LESSONS 'smoothstep on noise distributions creates bimodal bands' — topAlt = 0.4 + smoothstep(0.3,0.7,perlinSample)·0.55 produced 'visible curved stripes on cumulus bodies' because Perlin clusters at 0.5 and the smoothstep stretched the cluster into bimodal short/tall columns; lesson: use LINEAR remap or a full-width smoothstep 'anywhere noise drives a visible parameter'. (2) CLOUD_VISIBLE_ISSUES #3 root cause 2: the same formula made adjacent columns integrate to wildly different alphas → dot/speckle. The new plan replaces colSample with the B channel (good), but §4.2 keeps 'bake noise into the topHeight channel at map-bake time' — the BAKER must apply this lesson (no smoothstep-on-Perlin sharpening of tower-height noise), and the shader-side topHeight→topKm decode must stay linear. Constrains: §4.1 B channel, §4.2, §4.7 step 6.
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md §'smoothstep on noise distributions creates bimodal bands' | docs/CLOUD_VISIBLE_ISSUES.md #3 cause 2 | docs/CLOUD_TYPES_PLAN.md §4.2

## Coverage-cache family (case #1 + 3 follow-ons): per-step weather sampling is settled architecture; the hoisted covNear/Mid/Far taps survive ONLY as a load-bearing TSL If gate — do not remove, and audit ALL hoisted-value uses when extending to weatherAt [confirmed]
CLOUD_DEBUGGING_LESSONS case #1 (slab-midpoint tap → mirrored shells), follow-on 2026-05-13 (two-tap fails looking down), follow-on 2026-05-18 (three-tap → per-step; then the OUTER GATE still using 3-tap produced 'wireframe outlines / cloud portals'). General lesson recorded: 'when transitioning from a tap-based approximation to a per-step scheme, MUST audit ALL uses of the tap-based values'; 'the right architecture is per-step coverage sampling'. CLOUD_REVIEW_2026-07 'SMALLER FINDINGS' adds: the hoisted 3-tap samples survive only to feed a trivially-true If that is documented LOAD-BEARING TSL scope magic ('removing it zeroed alpha') — 'Leave unless someone re-falsifies that.' CLOUD_TYPES_PLAN §3.1 already flags 'hoisted covNear/Mid/Far (load-bearing TSL If — do not remove)'. Phase 1's weatherAt struct must (a) stay per-step, (b) keep the If wrapper (neutralise gates with trivially-true conditions, never delete — lessons §'TSL If(...) structure has scope side effects'), (c) grep every consumer of coverage-derived hoists (light volume densityAt, ground shadow, shell provider) when the tap becomes 4-channel. Falsification if artifacts appear: the one-line 'sample weather at current-step uv' substitution + DEBUG_VIZ alpha/iters/firstHit triad.
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md case #1 + follow-ons 2026-05-13/18 | docs/CLOUD_REVIEW_2026-07.md §SMALLER FINDINGS 'Dead-while-toggled code' | docs/CLOUD_TYPES_PLAN.md §5 Phase 1

## Phase 0 refactor hazards: TSL If-scope magic, .toVar aliasing, and the 'delete superseded paths' rule [confirmed]
Three refactor-killers for the cloudShared.ts consolidation: (1) Lessons §'TSL If(...) has scope side effects' — replacing If(cond,()=>{loop}) with a plain block silently produced α=0 everywhere; neutralise gates, never delete. (2) Memory feedback_tsl_var_aliasing — a node reading a mutable .toVar() that is reassigned before use evaluates to the NEW value (caused the invisible atmosphere); when extracting shared Fn helpers that read loop-mutable state, materialize inputs with .toVar(). (3) Case #21 update 2026-06-18: leaving the superseded opacity-only detail erosion running silently RE-INTRODUCED the edge speckle its replacement fixed — 'when a new mechanism supersedes an old one, DELETE the old path'; applies to cloudHeightProfile + its inline copy once the LUT lands (Phase 2), and to the old cloudType/covSpan code after Phase 1 A/B is done. Verification tool already prescribed: pixel-parity screenshots at the 3 canonical camera positions + full DEBUG_VIZ sweep (plan §5 Phase 0 — matches lessons practice).
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md §'TSL If structure has scope side effects' | docs/CLOUD_DEBUGGING_LESSONS.md case #21 UPDATE 2026-06-18 | memory feedback_tsl_var_aliasing | docs/CLOUD_TYPES_PLAN.md §5 Phase 0

## Mip rules are three-layered and touch every new texture in the plan: forced .level(0) in the marcher, auto-mip for shell/far consumers, and the three@0.183.2 Data3DTexture patch [confirmed]
(1) Case #2: auto-mip selection inside the marcher loop + per-pixel dither = camera-relative fabric bands visible in EVERY debug viz; fix is .level(int(0)) on every texture/texture3D tap in the loop — the new weather-map RGBA tap and the profile-LUT texture2D tap inside the marcher MUST be .level(0). (2) CLOUD_REVIEW DONE section: the SHELL's coverage tap was deliberately switched to AUTO-MIP (2026-07-05) because forced mip-0 is 'a marcher anti-banding hack the shell doesn't need' and caused far aliasing — so the new 4096×2048 weather map KTX2 must CARRY a mip chain (convert script --genmipmap) for shell/cirrus/far-field consumers, while marcher taps stay level 0; also watch the equirect anti-meridian seam over-blur line under auto-mip. (3) Case #16: three r183 never uploads Data3DTexture mips (GPU mips read zero) — patched via patches/three@0.183.2.patch in THREE places (src + both build bundles); re-run /dev/mip3d-test after ANY three upgrade. (4) Mip-content rule (2026-06-11 'Resolution of the mip dead-end'): box-filtered mips shift the Schneider Remap pass-rate — any new baked noise (e.g. the in-slab cirrus streak fallback of §4.5) needs variance renormalisation and a max-lod clamp (~2.5), and plan §3.1's 'DETAIL_MIP1_RENORM re-measure' requirement comes from this. Constrains: §4.1 encoding, §4.2 LUT sampling, §4.5, §4.7 step 7.
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md case #2, case #16, §'Resolution of the 2026-06-03 mip dead-end' | docs/CLOUD_REVIEW_2026-07.md §DONE 'shell coverage-texture sample switched to AUTO-MIP' + Bug B | docs/CLOUD_TYPES_PLAN.md §4.1, §4.5

## Case #13/#14 gate law: the skip→dense gate must test the SAME quantity the dense branch integrates — the LUT profile must feed BOTH, or low-coverage regions dense-lock again [confirmed]
Case #13: gating dense mode on the macro product (baseShapeCarved×coverage) instead of the remapped density dense-locked the march across zero-density 'phantom cloud' in low/mid coverage → budget death → the near/horizon/gap-in-between signature. Fix was probeShape = (carved − (1−profile))/profile using the SAME profile as the dense branch. When Phase 1/2 swap profile to coverage·LUT(altNorm,convectivity), the skip-mode probe, the dense branch, AND the If(profile>0.01) empty-band gate (which is what makes extra empty altitude bands cheap, per plan §3.1) must all read the NEW profile — a half-migrated gate reintroduces dense-lock or wireframe portals. Falsification tools that already exist: DEBUG_VIZ 'whyStop' (RED=budget), 'iters', 'maxProfile' (black = band never sampled) vs 'maxProbeShape' (field passes nothing) — the case #15/#16 discriminator pair built specifically to split sampling-vs-field hypotheses in one screenshot each.
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md cases #13, #14, #15 ('New permanent diagnostics') | docs/CLOUD_TYPES_PLAN.md §3.1 density chain, §5 Phase 1-2

## Case #20 density-model architecture is load-bearing: envelope IS the presence, noise only erodes; floaters are impossible only while shape ≤ profile — LUT rows must preserve this and the flat-base Remap-survival trick [confirmed]
Case #20: two-layer root cause of floaters/smooth-blobs/never-closing deck — (a) the dilation was a fill not an erosion, (b) 'shape = base + profile − 1' made NOISE the presence. Fixed with shape = saturate(profile − (1−base)·BASE_EROSION_K): 'shape ≤ profile ⇒ floaters impossible by construction; K<1 ⇒ high coverage fills gaps'. The LUT (Phase 2) replaces only the heightProfile factor inside profile — it must NOT change the composition form. Separately, SHAPE_PLAN Phase C anatomy (2026-06-16): the old gradual cumulus base ramp was ERASED by the value-erosion Remap (low profile → threshold ≈ 1 → noise can't pass) producing the 'lava lamp' floating-bottoms look; the fix was a SHARP base (smoothstep 0.04-0.16) that survives the Remap. LUT rows for convective types must be similarly sharp at the base or the flat-bottom anatomy silently regresses — plan §4.2 already names this ('port the existing anatomy') but the WHY (Remap survival, not just aesthetics) lives in SHAPE_PLAN. Falsification tools: DEBUG_VIZ 'floaterProbe', 'baseColumn', 'profile', and the [cloud base dist] histogram in noiseVolumes.ts (all kept in code for exactly this).
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md case #20 | docs/VOLUMETRIC_CLOUDS_SHAPE_PLAN.md §7 Phase C 'Anatomy: flat condensation base' | docs/CLOUD_TYPES_PLAN.md §4.2

## Column-tap removal is validated by case #19: WARP_AMPLITUDE=0 is a deliberate, hard-won decision (domain warp SHEARS clouds into filaments) — the tap's warp-vector side-use is dead by design, and warp must not be re-enabled for anti-tiling [confirmed]
Case #19 + SHAPE_PLAN 2026-06-15/16: the multi-day 'need an Alligator rewrite' conclusion was retracted — the stringy filaments were the DOMAIN WARP (source = base volume g/b/a at uColumnScale, km-scale gradient content, ±5 km displacement). Decision: WARP_AMPLITUDE/MIRROR=0 permanently; anti-tiling via detile was built, VALIDATED in /dev/cloud-slice, then PARKED (USE_DETILE=false) because 4-tap cost 60→15 fps and the crossfade altitude made tiling unhidable anyway. Consequences for the plan: (a) the per-step column texture3D tap's only live output is colSample→topAlt, so replacing topAlt with the B channel makes the tap deletable = the plan's −1 tex3D win (§4.2) — confirmed sound; grep for any other consumer of the column tap's gba before deleting. (b) STALENESS flag: SHAPE_PLAN's 'accept 20 km tiling at altitude' decision (option 3, 2026-06-16) was premised on the volumetric rendering over continental footprints to 1500-3000 km; the distance-based shell handoff has since LANDED (CLOUD_REVIEW DONE: marcher tExit capped at SHELL_HANDOFF_FAR_KM) — exactly the condition under which SHAPE_PLAN said 'option 1+2 become cheap and worthwhile'. Anti-tiling economics should be revisited once, but via detile/incommensurate scales, never warp. Also note DETILE_BLEND=2.0 is out of documented range — a trap on re-enable (review SMALLER FINDINGS).
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md case #19 | docs/VOLUMETRIC_CLOUDS_SHAPE_PLAN.md §'Anti-tiling reality check (2026-06-16)' | docs/CLOUD_REVIEW_2026-07.md §DONE 'Q1 near/far hand-off' + §SMALLER FINDINGS | docs/CLOUD_TYPES_PLAN.md §4.2 perf note

## Light-volume lockstep (cases #11/#17/#18): the LUT sharing fixes formula drift structurally, but a runtime weather-map/LUT swap leaves a STALE BAKE — shadows detach until re-bake is triggered [confirmed]
cloudLightVolume.ts hand-copies the whole coverage→type→topAlt→profile chain (densityAt L291-352 + cloudHeightProfileInline L666-694, per plan §3.1) — Phase 2's shared LUT eliminates that drift class. BUT cases #11/#17/#18 established the bake is heavily AMORTISED: re-runs only on voxel-window snap or >0.25° sun rotation, crossfaded over ~0.2 s, with side-B pipeline pre-warmed to hide WebGPU compile stutter. Nothing in that machinery watches the weather TEXTURE or profile function: flipping WEATHER_V2, swapping the synthetic test map, or regenerating the LUT at runtime leaves the baked transmittance field computed against the OLD density → globally wrong/detached shadows until the next incidental snap. Migration phases 1/2/4 must force a light-volume re-bake (both ping-pong sides) on any map/LUT/build-const change, and the A/B toggle should re-bake on flip. Also inherit case #18's hardening: never let a fade-in blend a NEVER-BAKED side (zero-init storage reads T=0 = full shadow). Verify: 'shadows still attached (light volume parity)' — already the plan's Phase 2 check; add 'after map swap' to it.
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md cases #11, #17, #18 (verification round) | docs/CLOUD_TYPES_PLAN.md §3.1 lockstep list, §5 Phase 2 verify

## Phase 3 per-type detail constraints from case #21: detail must reach the LIT density at the matching probe scale; edges must get LOW-freq or curl-wisp (never raw HF); ramps ride inside the existing amplitude fades [confirmed]
Case #21's five stacked causes constrain every §4.3 knob: (1) fine detail only in view-opacity = unlit speckle; the near self-shadow probe samples the SAME fine carve at DETAIL_SS_DIST ≈ detail scale — 'probe/sample DISTANCE = the feature scale that can self-shadow', so if convectivity ramps change effective detail character/scale, the probe composition must change in lockstep (this is WHY plan §4.3 makes the Phase-0 extraction of the duplicated fine-detail composition, primary L2032-2094 ↔ probe L2508-2558, a precondition). (2) Nubis p.109 verbatim: HF at edges = pockmarks; FINE_CARVE_GRADE_POW frequency-grading by profile is load-bearing — a convectivity-driven FINE_CARVE_STRENGTH ramp must multiply INTO the graded composite, not bypass the grading. (3) Case #22 fix 3: the whole fineDelta amplitude fades to 0 over DETAIL_FADE_NEAR→FAR (20→100 km) to kill distant scintillation — per-type ramps must live inside that fade; and case #15's rule applies: if any type's look EXISTS only via a faded term, it deletes at range ('distance fades on structure-creating terms are LOD deletions... band-limit by mip, never by amplitude' — noting the mip option is constrained by case #16). (4) WISP_AMOUNT ramp already anticipated by the code comment at ~L525 (plan §4.3 cites it). Falsification: A/B toggle per change + DEBUG_VIZ 'eroded'/'litShape'/'detailShadow' + the [cloud detail dist] histogram (SHAPE_PLAN header lists them as kept), plus the plan's own 'edge speckle regression watch (case #21)'.
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md case #21, case #22 fix 3, case #15 meta-lesson 1 | docs/VOLUMETRIC_CLOUDS_SHAPE_PLAN.md header status block | docs/CLOUD_TYPES_PLAN.md §4.3, §5 Phase 3

## Slab raise 14→16 km: km-anchoring solves anatomy, but slabLen-derived march tuning, the tExitSlab=tOuterFar rule, and light-volume vertical voxels all shift — verify with whyStop/iters, don't retune blind [confirmed]
Plan §3.1/§4.4 covers the profile side (alt01-fraction profiles would silently move stratus anatomy up; LUT is km-anchored). Additional doc-sourced constraints: (a) case #10/#22 march machinery derives from slabLen — lodCap = slabLen/(dtSkip·MIN_SAMPLES), dtSkipInBand, DENSE caps; a 15% thicker slab changes reach/budget behaviour especially on grazing rays; re-run DEBUG_VIZ 'whyStop' (budget/slab/opaque exit reasons) and 'iters' at the three canonical positions after the raise. (b) Case #22 issue 2: tExitSlab must remain tOuterFar ALWAYS (the inner-shell truncation bug made horizon clouds vanish from inside the band) — don't let constant refactoring reintroduce hitInner.select. (c) Case #18(b) SHELL-Y light volume: vertical voxel span = rMid + ly·(slab/2 + 1 km), 0.47 km voxels across the slab — derives automatically (plan says so) but the voxel count per slab drops slightly; check for the horizontal-brightness-zone banding signature returning. (d) Lockstep constants CLOUD_OUTER_ALTITUDE_KM ↔ earth.ts CLOUD_TOP_ALTITUDE_KM ↔ SHELL_ALTITUDE_KM(=14, the landed cloud shell sits at cloud-top radius) — the review's shell work added a THIRD lockstep site the plan's '2 constants' undercounts; uShellOpacity's deck fade (28→14 km) also keys off it.
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md case #10 Part B, #18(b), #22 issue 2 | docs/CLOUD_REVIEW_2026-07.md §DONE Phase 2 shell (SHELL_ALTITUDE_KM) | docs/CLOUD_TYPES_PLAN.md §4.4, §5 Phase 6

## Cirrus 2.5D shell (Phase 5) walks straight into the review's two OPEN shell bugs — Bug B's flicker mechanism (fresh-every-frame shell + sub-pixel content) applies verbatim, and Bug A warns against discrete-altitude profile sampling [confirmed]
CLOUD_REVIEW 'RESUME HERE' (2026-07-05, still open): Bug B — the existing cloud shell FLICKERS because it samples high-freq noise at .level(0) on a moving sphere with NO temporal smoothing and no detail fade; 'the shell renders fresh every frame, so ANY sub-pixel content flickers under motion. The clean far-field answer is only show smooth, footprint-resolved content (mipped coverage + faded/mipped noise).' The cirrus shell is the same architecture (second ExtraMeshDef sphere, scene geometry, no temporal pass) — its baked 3-channel cirrus texture and the Nubis 2.5D sharpening pow (which STEEPENS gradients, amplifying sub-pixel aliasing) must be auto-mipped and footprint-aware from day 1, and the density formula's coverage-driven pow sharpening should be validated at orbital footprints. Bug A — columnMacroCoverage's fixed alt01 {0.3,0.55,0.8} samples entirely missed the stratus band (profile nonzero only 0.05-0.25) → low-coverage clouds absent from the shell; ANY shell/far-field consumer that samples the new LUT profile at discrete altitudes inherits this failure mode, and Phase 2's new stratiform rows (mass hugging the top, altNorm 0.75-1.0) RELOCATE the nonzero band, so the existing shell's sample altitudes must be re-validated after the LUT swap even if Bug A is fixed before then. Also: falsified attempt recorded — BASE_EROSION_K 1.2→0.1 did NOT fix Bug A (don't re-try erosion-side fixes). Cirrus lighting: case #22.1 depressed-horizon terminator (μ_set = −√(1−(R/r)²)) matters MORE at 10-11 km; and case #18(c): before distance/coverage-gating any multiplicative shading term (e.g. the 1−0.5·coverage self-shadow proxy scaled by convectivity, plan §4.6), check its MEAN — fading a biased term paints a visible spatial boundary. New shell material also needs WebGPU pipeline pre-warm (case #18 hardening: lazy compile at first use = the exact pop).
ANCHORS: docs/CLOUD_REVIEW_2026-07.md §'RESUME HERE' Bugs A/B + Synthesis | docs/CLOUD_DEBUGGING_LESSONS.md case #22.1, case #18(c) + verification round | docs/CLOUD_TYPES_PLAN.md §4.5, §4.6

## SHAPE_PLAN Phase C planned items map 1:1 onto the new plan — supersession is clean except the type-distribution tuning note and the EVE contrast values worth keeping [confirmed]
SHAPE_PLAN §7 Phase C planned: (C.1) multi-channel synthesised map — superseded by §4.1; (C.2) per-type freeform coverage curves replacing the 3 smoothsteps, with envelope shaping bottom=pow(h,2)/top=pow(1−h,1.5) and EVE's multi-keyframe anvil curve cited — superseded by the §4.2 LUT (a strict generalisation: 'freeform coverageCurve' ≈ LUT row); (C.3) realistic type distribution 'fair-weather cumulus dominate, congestus occasional, Cb rare' — carried implicitly by ERA5 data (Phase 4) but NOT by the Phase 1-3 synthetic map; the synthetic 'genus test chart' should include a realism-fractions region so Phases 1-3 don't tune against an unrealistic sky; (C.4) 'optional second high layer (cirrus/altocumulus)' — now §4.5, no conflict. Still-useful concrete values from SHAPE_PLAN §3 not in the new plan: EVE/Blackrack per-type baseNoiseTiling 1852-4000 (non-harmonic anti-tiling values), Worley contrast=1.5/lift=0.5 on the base ('part of why their billows aren't mushy'), detailNoiseTiling=785 — reference points if per-type NOISE SCALE (not just blend weights) ever becomes a type axis. Also SHAPE_PLAN §9 'keep cloud-type curves and noise params data-driven (per-planet config)' matches §4.9's per-planet config, and VOLUMETRIC_CLOUDS_PLAN F1's VolumetricCloudConfig sketch (incl. cloudTypeProfile field, quality tiers) is the existing skeleton §4.9 should extend rather than reinvent.
ANCHORS: docs/VOLUMETRIC_CLOUDS_SHAPE_PLAN.md §3, §7 Phase C, §9 | docs/VOLUMETRIC_CLOUDS_PLAN.md §Phase F F1 | docs/CLOUD_TYPES_PLAN.md §4.1-4.2, §4.5, §4.9, §5 Phase 1

## Per-step cost model and gating economics from PERF + review docs support §4.10's accounting — with one nuance: the weather tap currently runs BEFORE any coverage gate [confirmed]
VOLUMETRIC_CLOUDS_PERF §1: '3D-texture fill rate is the bottleneck… everything else is ALU-cheap relative to the 3D taps' — supports §4.10's claim that reading .gba at the same 2D tap is free and per-type ALU ramps are cheap. Historical numbers in PERF §1/§5 are marked obsolete; use the review doc's method (3 canonical camera positions, one build-const flip at a time) for before/after measurement — the plan already adopts this (R7/§4.10). Review PERF lever #3 notes the per-step COLUMN tap currently runs before any coverage gate ('gate the column tap on coverage>0.01 BEFORE the 3D fetch — saves one 3D fetch per empty step on sky rays') — Phase 1's outright deletion of the column tap supersedes that lever and captures more than it (worth noting in the plan so the review lever is marked done-by-supersession). Review PERF lever #2 documents the designed fallback for the 800 m probe ('fade toward its MEAN absorption with distance, never toward 1' — the case #18(c) rule) — relevant if per-type work adds probe cost. Also budget context: ~8-9 texture3D + 1 texture2D per dense voxel today (plan §3.1, consistent with review lever #2's enumeration).
ANCHORS: docs/VOLUMETRIC_CLOUDS_PERF.md §1, STATUS NOTE 2026-06-11 | docs/CLOUD_REVIEW_2026-07.md §PERFORMANCE levers 2-3 | docs/CLOUD_TYPES_PLAN.md §4.10

## Feature-scale-vs-viewing-distance law constrains bake-time sub-grid detail injection (§4.7 step 6) [confirmed]
Lessons §'Feature-scale must match viewing-distance': 1 km cumulus puffs read as 'fine speckle from orbit'; the project deliberately uses 5 km bodies (uBaseScale=50) because 'cumulus feature scale should be chosen for the expected viewing-distance range, not physical realism; reference engines use 5-30 km cumulus for this reason.' The §4.7 bake injects sub-grid structure (Sc cell hints ~10-40 km — fine; coverage detail noise; tower-height noise) when upsampling 1440→4096: any injected feature much below ~5 km will be sub-pixel from the altitudes where the far shell carries the view, contributing shimmer (review Bug B mechanism) rather than realism. Keep injected detail ≥ the Sc-cell/base-body scale, and remember the runtime 'keeps only its existing 3D noise' for finer structure. Same law applies to the cirrus texture's fall-streak scale.
ANCHORS: docs/CLOUD_DEBUGGING_LESSONS.md §'Feature-scale must match viewing-distance' | docs/CLOUD_TYPES_PLAN.md §4.7 step 6, §4.5

## Process constraints the plan must inherit: measure-first, one-line falsifications, verify-current-code-state (files are uncommitted/in-flux), and null-results-are-measurements [confirmed]
Recurring meta-lessons that directly shape how the migration phases should run: (1) 'measure-first, no speculative fixes' is a standing user requirement (CLOUD_REVIEW 'Working method (do not skip)' + memory feedback_debugging); every phase's verify step should name the existing diagnostic before coding — the plan mostly does. (2) Wrong-hypothesis #1 (case #1): 'always verify the current code state before theorising' — earthClouds.ts/cloudCommon.ts/earth.ts are MODIFIED-UNCOMMITTED right now (ISSUE 2 Phase 2 shell work), and CLOUD_REVIEW says line numbers are 'was here at d251506' — every line anchor in CLOUD_TYPES_PLAN §3 will drift; re-grep symbols, never trust anchors. (3) 'An empirical one-line toggle beats a paragraph of geometry' (case #19) and 'a null result IS the measurement' (case #22, #21's 800 m probe test) — the CLAUDE.md hard-won-lessons pointer mandates binary one-line falsification tests early. (4) 'Three refuted fixes in a row means the bug is in a different LAYER' (case #15). (5) Case #7: MC variance at thin/low-density regions is a FUNDAMENTAL limit at the current sample budget — thin genera (cirrus-adjacent in-slab content, thin As sheets from low-v LUT rows) will inherently speckle more than thick decks; this is the standing argument FOR the 2.5D cirrus shell over the in-slab volumetric fallback, and should temper expectations when Phase 2 introduces thin stratiform rows.
ANCHORS: docs/CLOUD_REVIEW_2026-07.md §Working method | docs/CLOUD_DEBUGGING_LESSONS.md cases #1, #7, #15, #19, #22 meta-lessons | CLAUDE.md §Hard-won lessons

## Superseded/historical doc statements the new plan should not be constrained by (explicit all-clear list) [confirmed]
To prevent false constraints: (1) VOLUMETRIC_CLOUDS_PLAN's top-of-file 'we are explicitly NOT targeting Nubis³' + 'do not cross-check formulas against the 2023 PDF' predates the CloudTypesResearch extraction of all four decks with proper attribution; the new plan's selective use of 2022 LUT/cirrus and 2023 detail formulas is consistent with that doc's own carve-out ('Nubis³ shader-side up-rez tricks' allowed, authored-NVDF pipeline still out of scope per SHAPE_PLAN §6). (2) CLOUD_VISIBLE_ISSUES.md is marked HISTORICAL/superseded 2026-05-29 — only its topAlt-smoothstep lesson survives (folded into finding above). (3) PERF doc §1/§5 cost tables are marked historical; its Tier framing (half-res/temporal/shadow-map) all landed in different form. (4) SHAPE_PLAN's 'need an Alligator noise-generator rewrite' conclusion was formally RETRACTED (warp confound, case #19) — though Alligator WAS later adopted for cauliflower quality (case #21), the retraction history means 'noise generator can't do X' claims in old docs are unreliable. (5) VOLUMETRIC_CLOUDS_PLAN B2 Stage-1 ('derive cloudType from coverage — free, gives type variation') is precisely the shortcut the new plan exists to remove; the review confirms 'the code itself marks this deferred'. (6) The cone-march sections (C4, case #3) describe a path that is DEAD while USE_LIGHT_VOLUME=true (review SMALLER FINDINGS) — per-type MS_GAIN work (plan §4.3) targets the light-volume path, not the cone.
ANCHORS: docs/VOLUMETRIC_CLOUDS_PLAN.md header note + B2 | docs/CLOUD_VISIBLE_ISSUES.md header | docs/VOLUMETRIC_CLOUDS_SHAPE_PLAN.md 2026-06-16 pt.1 retraction | docs/CLOUD_REVIEW_2026-07.md §SMALLER FINDINGS

## RECOMMENDATIONS
- Add a 'Supersedes' note to CLOUD_TYPES_PLAN §4.1 naming SHAPE_PLAN §2.1 and VOLUMETRIC_CLOUDS_PLAN B2-Stage-2 (old R/G/B/A channel specs) as superseded, and add the procedural-first-vs-ERA5-baseline reversal to §6 open questions for Christian — it overturns a recorded 'locked' decision.
- In Phase 1, when extending the weather tap to weatherAt: keep per-step sampling, keep the load-bearing hoisted-If gate (neutralise, never delete), audit every consumer of the old hoists (light volume, ground shadow, shell provider), and force a light-volume re-bake (both ping-pong sides) on any WEATHER_V2 / map / LUT swap — the amortised bake will otherwise serve stale shadows.
- Bake-side rules to encode in bake_weather_map.py and the LUT generator: linear (not smoothstep) mapping of noise onto topHeight; sharp convective LUT bases (Remap-survival, the 0.04-0.16 lesson); injected sub-grid detail no finer than ~5 km except Sc-cell hints; the floater topHeight-clamp-at-low-coverage rule; a realism-fractions region (Cu-dominant, Cb rare) in the Phase-1 synthetic genus test chart.
- Texture/mip discipline for every new asset: marcher taps at .level(int(0)); shell/cirrus/far-field taps auto-mip with a real mip chain in the KTX2 (--genmipmap) and an anti-meridian seam check; re-run /dev/mip3d-test if three is ever upgraded (patches/three@0.183.2.patch); validate UASTC channel crosstalk as planned.
- Before Phase 5, resolve (or at least measure) the two OPEN shell bugs in CLOUD_REVIEW 'RESUME HERE' — the cirrus shell reuses the exact architecture that is currently flickering (Bug B) and missing low bands (Bug A), and Phase 2's relocated stratiform LUT rows will re-expose Bug A's discrete-altitude sampling in columnMacroCoverage even if fixed earlier.
- After the Phase-2 LUT swap and the Phase-6 slab raise, re-run the standing falsification battery: whyStop/iters (budget/gate regressions), maxProfile vs maxProbeShape (sampling vs field), floaterProbe + [cloud base dist] histogram (density-model regressions), shadow-attachment check (light-volume parity), pixel-parity screenshots at the 3 canonical positions; and update the third lockstep site (SHELL_ALTITUDE_KM + uShellOpacity fade) that the plan's '2 constants' count misses.
- Consider Frostbite's LUT G-channel (per-type erosion amount over height) as a v2 of §4.3 — same fetch as the density LUT, moves detail-character authoring from hardcoded ALU ramps into the regenerable LUT image.
- Add a one-line revisit note on anti-tiling: the SHELL_HANDOFF distance cap (landed 2026-07-04) satisfies SHAPE_PLAN's stated precondition for re-evaluating the parked detile / incommensurate-scale options; fix DETILE_BLEND=2.0 (out of documented range) before any re-enable.
